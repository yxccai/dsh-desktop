# DSH Desktop Aurora Theme

A bundled DSH Desktop theme preset with an aurora borealis palette: soft green and violet gradients, translucent raised surfaces, fresh green accents, and matching light/dark token overrides.

Install or enable it from DSH Settings → Plugins → Plugin Center. The visual contribution is applied by the trusted DSH Desktop client bridge and is fully reversible when the preset is disabled or uninstalled.
